[project]
name = "frontend"
version = "0.1.0"
description = ""
authors = [
    {name = "Eddiewav"}
]
requires-python = "^3.11"
dependencies = [
    "flet (==0.23.2)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
